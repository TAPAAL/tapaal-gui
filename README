To get eclipse and bzr plugin to work we need to install xmloutput, 
efter installing bzr and the bzr-eclipse pluing run:
bzr branch lp:bzr-xmloutput $HOME/.bazaar/plugins/xmloutput
After checking out the project (lp:tapn) we need to change the project type to a java project.
Close eclipse and open the .project file and change/add:
<buildSpec>
                <buildCommand>
                        <name>org.eclipse.jdt.core.javabuilder</name>
                        <arguments>
                        </arguments>
                </buildCommand>
        </buildSpec>
<natures>
                <nature>org.eclipse.jdt.core.javanature</nature>
</natures>

